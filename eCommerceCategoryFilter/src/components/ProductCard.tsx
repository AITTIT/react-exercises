import type { Product } from "../types/Product";

interface ProductProps {
    product: Product;
}

export default function ProductCard( { product } : ProductProps) {
    // Responsible only for displaying the image, title, and price.
    return <>adflkj</>
}